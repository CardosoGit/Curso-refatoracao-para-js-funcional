// if ( name === 'John' )   // ✓ ok
// if ( name == 'John' )    // ✗ evite

// if ( name !== 'John' )   // ✓ ok
// if ( name != 'John' )    // ✗ evite

const idade = '18'

if ( 18 === parseInt( idade ) ) console.log( '\n===' )
// if ( 18 == '18' ) console.log( '\n==' )